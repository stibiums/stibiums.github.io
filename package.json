{
  "devDependencies": {
    "@shopify/prettier-plugin-liquid": "^1.4.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6",
    "prettier": "^3.1.1"
  },
  "scripts": {
    "prepare": "husky",
    "format": "prettier --write ."
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  }
}
